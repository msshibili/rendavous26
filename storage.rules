rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /posters/{allPaths=**} {
      // Public read and write access for poster assets
      allow read, write: if true;
    }
  }
}
